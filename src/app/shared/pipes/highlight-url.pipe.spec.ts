import { HighlightUrlPipe } from './highlight-url.pipe';

describe('HighlightUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
